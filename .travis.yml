language: java
jdk: oraclejdk17
before_install:
  – chmod +x gradlew
script:
  - ./gradlew build
deploy:
  provider: heroku
  api_key:
    secure: $HEROKU_API_KEY
  app: fela-kuti
  on:
    branch: main